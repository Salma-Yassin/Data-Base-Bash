export LC_COLLATE=C
shopt -s extglob

function regex_checker {
    if [[ ${#1} -le 3 ]] || [[ ${#1} -gt 64 ]] || [[ ! $1  =~ ^[a-zA-Z][a-zA-Z0-9_]*$ ]]; then
        return 0
    else 
        return 1
    fi
}

